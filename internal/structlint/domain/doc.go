// Package domain описывает предметную область линтера структуры проекта: типы эталона
// (Template) и его правил, снимок дерева проекта (TreeSnapshot), нарушение (Violation) и
// вердикт (Verdict), а также узкие контракты доступа к данным (TemplateRepository, TreeScanner)
// и доменные sentinel-ошибки. Слой не зависит от файловой системы, транспорта и внешних
// библиотек проверки — только чистые типы и контракты (CLAR-002, CLAR-004).
package domain
